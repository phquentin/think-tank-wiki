# CLAUDE.md

This repo's authoritative schema lives in [`AGENTS.md`](AGENTS.md). It is the
single source of truth for how to maintain this wiki. Read it in full and
follow it for every session — including the workflows (§4) and hard rules (§8).

This `CLAUDE.md` is a **thin pointer only**; do not duplicate schema content
here. The `@AGENTS.md` import below loads the full schema into context.

@AGENTS.md
